<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chate">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".FirebaseHandler"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new_text"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_text_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OthersProfileActivity"></activity>
        <activity android:name=".UpdateAccountInfoActivity" />
        <activity android:name=".AccountInfoActivity" />
        <activity android:name=".ChooseUserActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ChatListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>